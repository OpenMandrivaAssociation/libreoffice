diff -up libreoffice-26.8.0.1/vcl/qt5/QtInstance.cxx.omv~ libreoffice-26.8.0.1/vcl/qt5/QtInstance.cxx
--- libreoffice-26.8.0.1/vcl/qt5/QtInstance.cxx.omv~	2026-07-13 12:47:46.000000000 +0200
+++ libreoffice-26.8.0.1/vcl/qt5/QtInstance.cxx	2026-07-22 19:10:38.000000000 +0200
@@ -361,7 +361,7 @@
 {
     // set the default application icon via desktop file just on Wayland,
     // as this otherwise overrides the individual desktop icons on X11.
-    if (QGuiApplication::platformName() == "wayland")
+    if (QGuiApplication::platformName().startsWith("wayland")) // wayland or wayland-egl
         QGuiApplication::setDesktopFileName(QStringLiteral("libreoffice-startcenter"));
     QGuiApplication::setLayoutDirection(AllSettings::GetLayoutRTL() ? Qt::RightToLeft
                                                                     : Qt::LeftToRight);
@@ -639,7 +639,8 @@
 Platform QtInstance::GetPlatform() const
 {
     const QString sPlatformName = QGuiApplication::platformName();
-    if (sPlatformName == u"wayland")
+    // "wayland" or "wayland-egl" (and any future wayland-* plugin name)
+    if (sPlatformName.startsWith(u"wayland"))
         return Platform::Wayland;
     if (sPlatformName == u"xcb")
         return Platform::Xcb;
diff -up libreoffice-26.8.0.1/vcl/qt5/QtFrame.cxx.omv~ libreoffice-26.8.0.1/vcl/qt5/QtFrame.cxx
--- libreoffice-26.8.0.1/vcl/qt5/QtFrame.cxx.omv~	2026-07-13 12:47:46.000000000 +0200
+++ libreoffice-26.8.0.1/vcl/qt5/QtFrame.cxx	2026-07-22 19:10:38.000000000 +0200
@@ -292,7 +292,8 @@
         aIcon = loadQPixmapIcon(IconHelper::GetInternalAppIconName(nIcon));
     m_pQWidget->window()->setWindowIcon(aIcon);
 
-    if (QGuiApplication::platformName() == "wayland" && m_pQWidget->window()->isVisible())
+    if (QGuiApplication::platformName().startsWith("wayland") // wayland or wayland-egl
+        && m_pQWidget->window()->isVisible())
     {
         // Qt currently doesn't provide API to directly set the app_id for a single
         // window/toplevel on Wayland, but the one set for the application is picked up
