diff -up libreoffice-26.8.0.1/vcl/source/app/salplug.cxx.7~ libreoffice-26.8.0.1/vcl/source/app/salplug.cxx
--- libreoffice-26.8.0.1/vcl/source/app/salplug.cxx.7~	2026-07-13 12:47:46.000000000 +0200
+++ libreoffice-26.8.0.1/vcl/source/app/salplug.cxx	2026-07-22 19:57:05.739745673 +0200
@@ -178,23 +178,93 @@ std::vector<OUString> autodetect_plugin_
 #endif
 
     std::vector<OUString> aPlugins;
-    if (eDesktop == DesktopType::LXQt || eDesktop == DesktopType::Plasma5 || eDesktop == DesktopType::Plasma6)
+
+    // Prefer gtk on GNOME-family desktops; prefer Qt/KF on Plasma/LXQt
+    // (do not prefer gtk over qt/kf by default).
+    if (eDesktop == DesktopType::GNOME || eDesktop == DesktopType::Xfce
+        || eDesktop == DesktopType::MATE)
     {
+#if ENABLE_GTK3
+        aPlugins.push_back(u"gtk3"_ustr);
+#endif
+#if ENABLE_QT6
+        aPlugins.push_back(u"qt6"_ustr);
+#endif
 #if ENABLE_KF6
-        if (eDesktop == DesktopType::Plasma6)
-            aPlugins.push_back(u"kf6"_ustr);
+        aPlugins.push_back(u"kf6"_ustr);
+#endif
+#if ENABLE_QT5
+        aPlugins.push_back(u"qt5"_ustr);
 #endif
 #if ENABLE_KF5
         aPlugins.push_back(u"kf5"_ustr);
 #endif
+    }
+    else if (eDesktop == DesktopType::Plasma6 || eDesktop == DesktopType::LXQt)
+    {
+#if ENABLE_KF6
+        aPlugins.push_back(u"kf6"_ustr);
+#endif
+#if ENABLE_QT6
+        aPlugins.push_back(u"qt6"_ustr);
+#endif
+#if ENABLE_KF5
+        aPlugins.push_back(u"kf5"_ustr);
+#endif
+#if ENABLE_QT5
+        aPlugins.push_back(u"qt5"_ustr);
+#endif
 #if ENABLE_GTK3_KDE5
         aPlugins.push_back(u"gtk3_kde5"_ustr);
 #endif
+#if ENABLE_GTK3
+        aPlugins.push_back(u"gtk3"_ustr);
+#endif
     }
-
+    else if (eDesktop == DesktopType::Plasma5)
+    {
+#if ENABLE_KF5
+        aPlugins.push_back(u"kf5"_ustr);
+#endif
+#if ENABLE_QT5
+        aPlugins.push_back(u"qt5"_ustr);
+#endif
+#if ENABLE_KF6
+        aPlugins.push_back(u"kf6"_ustr);
+#endif
+#if ENABLE_QT6
+        aPlugins.push_back(u"qt6"_ustr);
+#endif
+#if ENABLE_GTK3_KDE5
+        aPlugins.push_back(u"gtk3_kde5"_ustr);
+#endif
+#if ENABLE_GTK3
+        aPlugins.push_back(u"gtk3"_ustr);
+#endif
+    }
+    else
+    {
+        // Default: prefer Qt/KF over GTK
+#if ENABLE_QT6
+        aPlugins.push_back(u"qt6"_ustr);
+#endif
+#if ENABLE_KF6
+        aPlugins.push_back(u"kf6"_ustr);
+#endif
+#if ENABLE_QT5
+        aPlugins.push_back(u"qt5"_ustr);
+#endif
+#if ENABLE_KF5
+        aPlugins.push_back(u"kf5"_ustr);
+#endif
+#if ENABLE_GTK3_KDE5
+        aPlugins.push_back(u"gtk3_kde5"_ustr);
+#endif
 #if ENABLE_GTK3
-    aPlugins.push_back(u"gtk3"_ustr);
+        aPlugins.push_back(u"gtk3"_ustr);
 #endif
+    }
+
 #if ENABLE_GEN
     aPlugins.push_back(u"gen"_ustr);
 #endif
@@ -267,7 +337,7 @@ SalInstance *CreateSalInstance()
     }
 #endif
 
-    // fallback, try everything
+    // fallback, try everything (prefer Qt/KF over GTK)
     static constexpr OUString aPlugins[] = {
 #if defined(MACOSX)
         u"osx"_ustr,
@@ -275,27 +345,27 @@ SalInstance *CreateSalInstance()
 #if ENABLE_WIN
         u"win"_ustr,
 #endif
-#if ENABLE_GTK3
-        u"gtk3"_ustr,
+#if ENABLE_KF6
+        u"kf6"_ustr,
+#endif
+#if ENABLE_QT6
+        u"qt6"_ustr,
 #endif
 #if ENABLE_KF5
         u"kf5"_ustr,
 #endif
+#if ENABLE_QT5
+        u"qt5"_ustr,
+#endif
 #if ENABLE_GTK3_KDE5
         u"gtk3_kde5"_ustr,
 #endif
+#if ENABLE_GTK3
+        u"gtk3"_ustr,
+#endif
 #if ENABLE_GEN
         u"gen"_ustr,
 #endif
-#if ENABLE_QT5
-        u"qt5"_ustr,
-#endif
-#if ENABLE_KF6
-        u"kf6"_ustr,
-#endif
-#if ENABLE_QT6
-        u"qt6"_ustr,
-#endif
     };
 
     for (const OUString& rPlugin : aPlugins)
